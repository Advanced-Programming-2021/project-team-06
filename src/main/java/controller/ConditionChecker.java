package controller;
import models.cards.Card;
import java.util.regex.Matcher;

public class ConditionChecker {
    public boolean check(String condition, Matcher actionMatcher, Card clientCard) {
        return true;
    }
}
